# 🔴 Connect 4 CLI (TypeScript)

## Connect Four game built to run directly in your terminal.

## 🚀 Getting Started

### 1. Prerequisites

Ensure you have [Node.js](https://nodejs.org/) installed on your machine.

### 2. Installation

Clone the repository and install the dependencies:

```bash
npm install
npm start run
```
